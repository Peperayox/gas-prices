import React from 'react';

const World = () => {
  return (
    <h3>
      Hi there, React Redux and Router is working! Now there used to be cards
      like in Europe but my API ran out of request and had to switch to a more
      limited API;(
    </h3>
  );
};

export default World;
